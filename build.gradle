java {
    toolchain.languageVersion = JavaLanguageVersion.of(17)
}

userdev {
    minecraftVersion = "1.21.1"
    neoforgeVersion = "20.1.67-beta"

    runs {
        client
        server
        data
    }
}

group = 'emanon.collars'
version = '1.0.0'

repositories {
    mavenCentral()
    maven { url = "https://maven.neoforged.net/releases" }
}

dependencies {
    implementation "net.neoforged:neoforge:20.1.67-beta"
}
